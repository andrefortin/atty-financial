# Prime

Execute the `Run`, `Read` and `Report` sections to understand the codebase then summarize your understanding.

## Run

git ls-files

## Read

README.md
docs/\*.md (if exists)
apps/backend/docs/\*.md (if exists)
apps/frontend/docs/\*.md (if exists)
apps/mcp-server/\*.md (if exists)
apps/worker/docs/\*.md (if exists)
apps/backend/README.md (if exists)
apps/frontend/README.md (if exists)
apps/mcp-server/README.md (if exists)
apps/worker/README.md (if exists)
ai_docs/\*.md (if exists)
documents/\*.md (if exists)

## Report

Summarize your understanding of the codebase.
