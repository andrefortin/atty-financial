# Disable workspace-aware behavior for npm commands that don't support it
# This fixes the ENOWORKSPACES error when Next.js calls npm config get registry

# Set registry directly to avoid config lookup
registry=https://registry.npmjs.org/

# Auto-confirm prompts for CI/CD
progress=false
